# 野球メモ v3.01.14

## 修正内容
- 起動直後に「打者・走者・アウト・得点を選択・確認」が表示される問題を修正。
- 原因は v3.01.13 の確認モーダルCSSが `display:flex!important` を指定し、HTML初期値の `style="display:none"` を上書きしていたこと。
- `display` はCSSから指定せず、HTML/JavaScript側の表示制御をそのまま利用。
- 確認画面は下部4タブ（📝記録 / 👥チーム / 📋オーダー / 📊データ）の上に表示するため、モーダルを `z-index:1000` に固定。
- 確認画面の下端を88px空け、確認画面の「確定」「戻る」が下部4タブと重ならないようにした。
- 走者など確認項目が多い場合は `runnerConfirmSummary` のみ上下スクロール。
- 進塁結果エンジン、走者状態、得点、打席記録などのロジックは変更していない。
- BASELINE ZIPは変更していない。
