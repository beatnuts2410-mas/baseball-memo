# 野球メモ v3.03.0

## 選択確認画面の根本デバッグ・再構築

### 修正した競合
- 旧 v2.78〜v3.02.x の「runnerConfirmModal」用CSSが多数重複していたため、確認モーダルCSSを一本化。
- 旧CSSに存在した `overflow:hidden` / 高さ固定 / 画面内1画面固定の競合を撤去。
- HTML末尾の `</html>` 後にCSSが残る不正な構造を修正し、全CSSをhead内に整理。
- 確認モーダルの最終CSSを1か所だけが所有する構造に変更。

### 動作系の修正
- `pa()` の手動確認ルートでは `render()` を呼ばず、確認モーダルを直接描画。
- 確認内容の描画に失敗しても、空白モーダルにせず再表示ボタン付きのエラー表示を出す安全策を追加。
- 打者進塁先、走者進塁先、アウトカウント、得点、確定予定を必ず生成。
- 確定・戻るボタンを常時表示。
- 確認画面は高さ不足時のみ縦スクロール。
- 走塁プレー画面・走者修正画面も縦スクロール可能に統一。

### データ重複の修正
- 同一の `storeAdvanceResult()` が連続して実行されるボーク記録の重複を削除。
- 打席結果側に存在していた同一記録の重複も削除済み。

### 検証
- JavaScript `node --check`：PASS
- `runnerConfirmModal`：1個
- `openRunnerConfirm`：1個
- `renderRunnerConfirmEditor`：1個
- `confirmRunnerAdvance`：1個
- 旧確認画面CSS：撤去
- CSSはhead内へ整理
- body/html後へのCSS残留：なし

※実機スマホでのタップ確認は、この環境では実機を直接操作できないため未確認。
