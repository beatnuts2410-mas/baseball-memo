# 野球メモ 共有リンク版

- QR画像を共有する方式を終了。
- 試合結果を圧縮したURLそのものを生成し、LINE・SNS等へ送信できる方式へ変更。
- 共有URLは `?m4=` のURLセーフなデータ形式を使用。
- 受信側はURLを開くと「試合データ」に直行。
- 共有された試合画面では、QR/SNS作成部分を表示せず「試合結果をマイアプリに保存」を表示。
- 旧 `m3` 共有URLも読み込み対応を残しています。
- JavaScript構文チェック済み。

※実機のLINE送受信・スマホでのタップ開通は、この環境からは物理確認していません。
