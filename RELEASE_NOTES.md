# 西春野球ノート v2.49

- 写真保存が失敗する原因になっていた巨大Canvasの上限超過を修正。
- 10倍を基準にしつつ、ブラウザのCanvas安全上限内へ自動調整。
- PNG生成に失敗した場合はJPEGへ自動フォールバック。
- 保存用リンクをDOMへ一時追加して確実にダウンロード開始。
- SNS共有も画像Blobの生成失敗時に安全にフォールバック。
- 自チーム中心の試合結果画像レイアウトを維持。
