# 野球メモ v2.72

- v2.71をベースに再構成。
- 現場で操作する試合記録画面は「最初から」のレイアウトを基準に統一。
- 「途中から」で打順確認中の画面も、打者・打席結果・走者プレーの配置を最初からの画面構成に統一。
- 現場入力画面に表示していた明示的な「スコア」カード／「イニングスコア」カードを削除。
- 既存のデータ処理・記録ロジックは今回の範囲では変更しない。

## v2.72.2
- v2.72をベースに、**グラウンド表示部分だけ**を再構築。
- 送付された454×340のグラウンド画像を背景として使用。
- 1塁・2塁・3塁の走者位置を画像上に固定し、走者名を重ねて表示。
- 「走者修正」も同じグラウンド画像を使用し、塁ごとの選択状態を画像上で確認可能にした。
- それ以外の試合記録・共有リンク・入力画面の基本レイアウトは変更しない。
- QR専用の旧アセット `qr-logo.png` を削除。
