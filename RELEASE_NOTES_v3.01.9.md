# 野球メモ v3.01.9

## 修正内容
- 「すべて確認して確定 → 次の打順へ」が下部固定タブに遮られないよう、確認モーダルを最前面化。
- 「進塁先を選択」の走者プレーモーダルも下部固定タブより前面化し、進塁先ボタンをタップ可能に修正。
- ボタン・セレクトのタッチ操作を明示的に有効化。
- ホーム画面右上アイコンを68px（小画面62px）に調整し、タイトル・サブタイトルとの重なりを防止。
- 既存のタップ方式・進塁結果エンジン・データ構造は変更しない。

## 確認
- index.html のインラインJavaScript全ブロックを `node --check` で構文確認済み。
