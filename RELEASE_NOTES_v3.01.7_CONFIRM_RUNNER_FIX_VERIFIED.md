# 野球メモ v3.01.7｜打席結果・走塁プレー確定処理修正版

## 修正対象
- 打席結果（単打・二塁打・三塁打・犠打・犠飛・内野ゴロ・ライナー・内野フライ・外野フライ）の確認画面
- 走塁プレー（盗塁等）の状態遷移
- 「確定 → 次の打者」処理

## 修正内容
1. 現在の試合データを確定処理入口で正規化し、旧/不完全データでも `innings / pa / runners / runnerNames / runnerMoves / advanceResults / lineup` を安全に初期化。
2. 確定処理にロールバック用スナップショットを追加。途中で例外が発生しても無反応状態を残さず、確定前状態へ戻してモーダルを閉じる。
3. 走者プレーの入口（走者選択・盗塁判定・進塁先確定）でも同じ試合データ正規化を適用。
4. 打席結果処理の入口にも同じ正規化を適用。
5. 既存の進塁結果ルール・タップ操作・①〜⑦の優先順位は変更しない。
6. 重複していた一部の `storeAdvanceResult` 呼び出しを整理し、同一イベントが二重記録される状態を防止。

## こちらで実施した検証
- HTML内JavaScript構文チェック：PASS
- 単打・走者なし → 確認 → 確定 → 次打者：PASS
- 旧形式に近い不完全な試合データ → 単打 → 確定：PASS
- 盗塁セーフ → 2塁：PASS
- 盗塁アウト → アウトカウント増加：PASS
- ZIP展開構造確認：PASS
