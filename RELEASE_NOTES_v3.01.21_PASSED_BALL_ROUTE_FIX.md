# 野球メモ v3.01.21

## 捕逸（パスボール）の呼び出し先修正
v3.01.20で追加した「全ランナー進塁 / 個別に選択」対応の専用画面 `openPassedBallSteps()` が実際の捕逸ボタンから呼ばれていない箇所を修正。

### 修正内容
- 走者プレー → 捕逸（パスボール）を押した時、旧 `openRunnerEventSteps('捕逸')` ではなく `openPassedBallSteps()` を必ず呼び出す。
- 既存の3人走者対応UIをそのまま使用。
- 「全ランナー進塁」→ 各走者の進塁内容確認 → 決定。
- 「個別に選択」→ 1人以上の走者を複数選択 → 選択した各走者の進塁先を選択 → 決定。
- 既存の進塁結果エンジンへの反映は変更しない。
- 盗塁、暴投、ボーク、打撃結果、確認画面、下部4タブは変更しない。
- BASELINEは変更しない。
