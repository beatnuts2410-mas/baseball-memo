# 野球メモ v3.01.22

## 変更内容
- 暴投（ワイルドピッチ）を、捕逸（パスボール）と同じ複数走者対応UIへ変更。
- 「全ランナー進塁」または「個別に選択」を選択可能。
- 個別選択では複数走者を選択し、各走者の進塁先を選択してから「決定」。
- 3塁走者の本塁・得点にも対応。
- 確定後の記録種別は「暴投」として保存し、既存の進塁結果エンジンへ渡す。
- 捕逸の既存フローは変更なし。
- BASELINEは変更していない。
